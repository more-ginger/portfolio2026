node_modules

# SvelteKit generated files
.svelte-kit
build

# Netlify adapter output (the serverless render function) — regenerated by
# every build, so it should never be committed.
.netlify

# Vite
.vite

# Env files
.env
.env.*
!.env.example

# OS / editor
.DS_Store
